{
  "name": "root",
  "private": true,
  "devDependencies": {
    "@commitlint/cli": "^9.0.1",
    "@commitlint/config-conventional": "^9.0.1",
    "husky": "^4.3.0",
    "lerna": "^3.22.1"
  },
  "scripts": {
    "test": "lerna run test --scope @mists/nestjs-tracing*"
  },
  "husky": {
    "hooks": {
      "commit-msg": "commitlint -E HUSKY_GIT_PARAMS"
    }
  }
}
